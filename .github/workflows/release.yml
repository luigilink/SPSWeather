# This is the SPSWeather CI Release workflow to create release from tag push.
# Triggered by pushing a v* tag (e.g. v2.0.0). Packages the contents of the
# src/ folder into a ZIP (so the archive extracts straight to Config/, Modules/
# and SPSWeather.ps1, with no src/ wrapper) and publishes a GitHub Release using
# RELEASE-NOTES.md as the body.

name: SPSWeather CI Release

on:
  push:
    # Sequence of patterns matched against refs/tags
    tags:
      - "v*" # Push events to matching v*, i.e. v1.0, v20.15.10

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # Checkout code
      - name: Checkout code
        id: checkout_code
        uses: actions/checkout@v4
      # Create a ZIP file with project name and tag version
      - name: Create ZIP file of src contents
        run: |
          zip_name="SPSWeather-${{ github.ref_name }}.zip"
          ( cd src && zip -r "../$zip_name" . )
        shell: bash
      # Create Release with tag version
      - name: Create GitHub Release
        id: create_release
        uses: softprops/action-gh-release@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          draft: false
          prerelease: false
          body_path: RELEASE-NOTES.md
          files: |
            LICENSE
            SPSWeather-${{ github.ref_name }}.zip
